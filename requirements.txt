# Minimal requirements for text only mode
flask==2.2.3
flask-cors==3.0.10
werkzeug==2.2.3
google-generativeai==0.3.1
gunicorn==20.1.0

# Optional dependencies - comment these out to reduce memory usage
# kokoro>=0.9.4 ; platform_system != "Windows"
# soundfile>=0.12.1 ; platform_system != "Windows"
# torch>=2.0.0 ; platform_system != "Windows"
