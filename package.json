{
  "name": "interview-ai",
  "private": true,
  "scripts": {
    "build": "cd my-voice-assistant && npm install && npm run build",
    "start": "./run.sh",
    "dev": "./run_local.sh"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
