{
  "name": "forge",
  "private": true,
  "scripts": {
    "build": "cd my-voice-assistant && npm install && npm run build"
  },
  "dependencies": {
    "create-react-app": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0"
  }
}
