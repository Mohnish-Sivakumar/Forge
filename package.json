{
  "name": "forge",
  "private": true,
  "scripts": {
    "build": "cd my-voice-assistant && npm install && npm run build",
    "start": "./run.sh"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
